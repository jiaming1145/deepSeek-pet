# GPT closure review 3 (chat-request.ts, key-request.ts, bubble/main.ts; main 5d1bc33) — VERDICT: block | major:1 → RESIDUAL-2 lane

GC3-1 [major] key-request.ts createKeyRequest.onVerdict — a queued key prompt never flushes when a system flag clears while the user-hidden flag stays set: onVerdict returns whenever the AGGREGATE verdict is hidden, although decideKeyRequest permits opening under user-only hiding. Scenario: {locked:true,user:true} queues an auth prompt; unlock → hidden still true (user) → prompt queued forever. Fix: on every verdict change re-run decideKeyRequest against the individual flags and flush when it returns 'open'; clear `pending` BEFORE invoking show (exactly-once under re-entry). Test: queue under {locked,user}, clear locked, onVerdict → shown exactly once while user stays true.
Also asked for (integration tests): chat request before wiring replayed through the opener; speech: hidden → idle → visible → controller cleared, no stale band, no acks while hidden (= GC2-3).

Closed: decideChatRequest reads locked/suspended/fullscreen individually; system hiding outranks user; user-only → reveal; decideKeyRequest queues under each system flag and opens under user-only hiding; queued key reasons latest-wins and cleared before show; bubble/main.ts wires shell:visibility → hint.dismiss + speech.pause / speech.resume; the paused-then-idle cancellation path is reachable (GC2-3 stands).
