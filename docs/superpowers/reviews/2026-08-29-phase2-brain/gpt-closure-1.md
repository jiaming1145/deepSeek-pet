# GPT closure review 1 of the merged fix wave (main 5d1bc33; gpt-5.6-sol, xhigh, 2026-08-30) — VERDICT: block | major:5 minor:2 → fixed by the RESIDUAL lane (fix-residual-brief.md, IDs GC-1..GC-7)

GC-1 [major] history.ts window/trim/visibleRows — 32k safety truncation: window() removes the oldest prefix before planTrim sees it; trim() summarises only plan.drop from the truncated window then advances last_trim_id to the exact cutoff, excluding the omitted prefix (ids 1–7 lost when plan.drop = 8–22). Fix: summarise every row base < id <= cutoff incl. omitted ones; bounded sequential chunks if too large; do not advance until the whole prefix is incorporated.
GC-2 [major] turn.ts settleEmpty — sets settled=true before playback and commits the full canned row before turnShown(); send()/cancel()/bubbleCrashed() cannot retire it; delayed commitUser can emit the canned sentence after a newer turn. Fix: same streamFinished/reported/settled lifecycle as normal replies; defer the append until turnShown(); keep kind='system'.
GC-3 [major] turn.ts enqueue/commitUser/send — write chain erases success/failure (`this.writes.then(work).catch(...)` + `committed=true` before the write runs); disk-full/db failures become successful terminal outcomes with missing history. Fix: operation promise (keeps rejection) + caught barrier promise (ordering only); userCommit none/pending/durable/failed; superseding turn awaits pending and carries text only if failed; failed assistant append → explicit persistence-error path.
GC-4 [major] deepseek.ts parseSseLine/streamOnce — malformed JSON frames skipped; delta → malformed → [DONE] completes with content missing. Fix: malformed non-empty data frame = DeepSeekError('server'/'network') after logging only length; detect provider error objects in 200 SSE; never include payload.
GC-5 [major] brain-service.ts key:test + rebuildClient — result not tied to the key/generation tested (literal A recorded as stored B's status; late A overwrites after rotation to B). Fix: generation + key fingerprint; abort prior tests on key change; update lastTest only if generation current and tested key == stored key; literal-key tests never update stored status.
GC-6 [minor] brain-service.ts dispose — in-flight key tests untracked; can mutate lastTest / refreshKeyStatus after dispose. Fix: track controllers/promises; abort + drain on dispose; disposed/generation guards.
GC-7 [minor] deepseek.ts testKey — ignores readBodyBounded().truncated → ok:true for an oversized 200 body. Fix: truncated → server failure; validate JSON shape.

Closed (GPT-verified in the supplied files): turn.ts normal replies (write-chain ordering, awaitable cancel returning retirement work, reported/settled separation, mid-stream failure persisting acknowledged sentences, CX-1 deferral + retire-on-supersede, bounded regeneration); deepseek.ts (bounded bodies + idle timeouts, cancelled bodies, EOF-before-[DONE] = network, status-derived messages, redacted detail, length-only warnings); brain-service.ts (hint TTL vs idle fallback via hideOwed; bubble-crash retire of streamed replies); history.ts (serialised trims, exact cutoff id, pointer check before commit, closing fence after summarise); index.ts supplied portion (bubble show loses to the verdict; every hide routes through bubbleHidden()).
Not verified by GPT (attachment budget): quit.ts, speech.ts, chat-window.ts, db.ts, index.ts tail (requestChat/createBeforeQuit), first-message methods — follow-up review dispatched (gpt-closure-2.md).
