# GPT closure review 2 (files the first pass could not attach; main 5d1bc33; gpt-5.6-sol, xhigh) — VERDICT: block | major:4 minor:1 → RESIDUAL-2 lane (fix-residual2-brief.md, IDs GC2-n)

GC2-1 [major] quit.ts createBeforeQuit.handler — teardownSync() is the only shutdown step invoked unguarded; if it throws, phase stays 'draining', run() never starts, every later before-quit is prevented forever. Fix: guarded step + start run() regardless; finally-style guarantee that phase reaches 'done' and quit() is attempted. Test: teardownSync throws → drain, teardownAfterDrain, closeDb, quit each attempted once; phase done.
GC2-2 [major] index.ts app.on('second-instance') — does not route through requestChat and does not open chat (only clears the user flag), contradicting the single-opener rule. Fix: requestChat('second-instance', true); the event can precede ready → keep a pending request / late-bound delegate replayed once wiring completes. Tests: after ready → opens through the gate; before ready → replayed.
GC2-3 [major] speech.ts SpeechController.onState — idle received while paused and unfinished wedges the controller: finishTurn() returns because !isVisible, leaving finished=false, turnId set, current=null, hideAt=null; resume() shows the band with no continuation path. Fix: a cancellation finaliser distinct from finishTurn(): invalidate timers, clear queue/current/beat, mark finished + inactive, hide WITHOUT sending playback:turnDone, resolve waiters. Test: pause → unfinished idle → resume → inactive, hidden, timer-free, no sentenceDone/turnDone.
GC2-4 [major] speech.ts SpeechController.complete — no visibility guard: complete() after pause() commits current + queued sentences and emits playback:sentenceDone while hidden (CX-1 would persist unseen text as shown). Fix: no-op while !isVisible (or defer until resume); never commitSentence while paused. Test.
GC2-5 [minor] chat-window.ts closeChat / pendingOpened — closing during initial load does not cancel pendingOpened; did-finish-load later sends chat:opened (+ focusComposer) to a dismissed window. Fix: clearPendingOpened() from closeChat(), incl. when already hidden. Test.
Not yet verified by GPT: chat-request.ts, key-request.ts (closure 3 requested).

Closed: closeDb throwing no longer wedges quit; repeated before-quit prevented without duplicate drains; quit order dispose → windows → db → quit; tray/hotkey/chat:open all route through requestChat's single openChat; chatOpenSenders updated on window recreation; key-window auth requests via keyGate; chat/key loadURL observed; chat render-process death → hide, clear, reload once, escalate; chat focus only via openChat; schema_version/last_trim_id canonical validation; transactional migration + close-on-failure; pause/resume suppresses timers/acks, beats keep remainder, linger restarts; linger arms token-protected.
