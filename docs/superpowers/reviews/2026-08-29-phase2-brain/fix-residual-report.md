# Fix lane RESIDUAL — report (GC-1..GC-7, GPT closure review 1)

Worktree `D:\ds\.claude\worktrees\wf_bdc9c0e6-32f-1`, branch `worktree-wf_bdc9c0e6-32f-1`, base `ca437f3` (main). Six fix commits on the branch, nothing on main. Every finding in `fix-residual-brief.md` reproduced (a red test written first, run against the old code, then the fix) — none turned out to be NOT A BUG.

Gate (all run in the worktree after the last commit):

| Check | Result |
|---|---|
| `pnpm test` | 70 files, **885 passed**, 2 skipped (baseline before this lane: 862 passed) |
| `pnpm -r --if-present typecheck` | protocol, brain, stage, memory, desktop — all Done, exit 0 |
| `pnpm --filter @ds/desktop build` | `✓ built` |
| key-pattern scan (`sk-[A-Za-z0-9]{20,}`) over every touched file | no hits (the pre-commit hook also rejected one draft fixture; fixed by building the fixture at runtime) |

Never read `~/.ds/deepseek.key`, never set `DEEPSEEK_API_KEY`, no test constructs a network-bound DeepSeekClient (the literal-key probe is now injectable — see GC-5).

## Per finding

### GC-1 [major] `packages/memory/src/history.ts` — trim loses the safety-truncated prefix
- **Fixed how.** `trim()` still resolves the exact cutoff id from `plan.drop` before the await (A-33). It then loads **every** row `base < id <= cutoff` with ids (`rowsBetween`, history.ts:320) — including the oldest rows `visibleRows()` had cut above `HISTORY_WINDOW_SAFETY_TOKENS` — splits them with `chunkByTokens` (history.ts:364) into sequential chunks of at most `TRIM_CHUNK_TOKENS = 24_000` estimated tokens (history.ts:32; one oversized row is its own chunk, never split), and chains the summariser through them (`next = await summarize(next, chunk)`), applying `capSummary` between chunks so a later chunk sees what the store would keep. `last_trim_id` and the summary are written in **one** transaction only after the whole prefix is incorporated; the transaction re-checks the pointer snapshot AND the id-bearing prefix snapshot (`prefixStillMatches`, history.ts:333). A failing chunk writes nothing (pointer and summary untouched). Decision: intermediate summaries live in memory rather than being committed per chunk, so a mid-way failure cannot leave a half-updated summary next to an unmoved pointer.
- **Tests** (`packages/memory/src/history.test.ts`): `GC-1: a trim behind the safety truncation summarises EVERY row from the old pointer to the cutoff, once each` (60 × 600-token rows, window starts at id 8, plan.drop = 8..22 → summariser input is exactly ids 1..22 in order, once each, every chunk ≤ 24k, pointer = 22); `GC-1: an oversized prefix is summarised in bounded sequential chunks, chained through the summary, one commit` (100 rows, prefix 1..62 ≈ 37k tokens → chunks of 39 + 23 rows, second call receives the first chunk's summary, one commit at 62); `GC-1: a failing later chunk advances nothing`. The CX-5 test's assertion `summarizeCalls[0][1] === plan.drop` encoded the bug and now asserts the input **ends with** plan.drop and has 22 rows.
- **Commit** `28577f1 fix(memory): summarise the whole retired prefix, chunked, before advancing last_trim_id (GC-1)`.

### GC-2 [major] `packages/brain/src/turn.ts` — canned reply settled before playback
- **Fixed how.** `settleEmpty` (turn.ts ~570) no longer sets `settled`, no longer awaits the user row before speaking, and no longer appends the canned row itself. It sets `turn.assistantKind = 'system'` and `turn.outcome = 'empty'`, places the canned sentence in `pending` and calls the ordinary `release()` (user row enqueued, `speaking`, sentence emitted), then the new shared `settleFinished()` (turn.ts:534, the tail of the old `settleNormal`): `streamFinished = true`, turnDone + metrics once, the assistant row deferred to `turnShown()` (CX-1 / A-27). `commitAssistant`/`persistShown` write `kind: turn.assistantKind`, so both the deferred full row and an interrupted acknowledged prefix keep `kind='system'`. `retire()` and `finish()` record `turn.outcome` so a canned turn retired mid-playback still carries `errorCode:'empty'`. The "delayed commitUser emits the canned sentence after a newer turn" path is gone: nothing is awaited between the emptiness decision and the emit.
- **Tests** (`packages/brain/src/turn.test.ts`, describe "the canned reply follows the normal lifecycle (GC-2)"): send() after the canned sentence, before turnShown → no full row for t1, only t2's rows, metrics `[t1 empty, t2 null]`, a late `turnShown('t1')` writes nothing; cancel() after `sentenceShown` but before turnShown → exactly the canned sentence as `{kind:'system', interrupted:true}`; cancel() while the canned path awaits a gated user append → `cancel()` resolves only after the gate opens and both rows + metrics have landed; cancel() with nothing acknowledged → no assistant row, one turnDone, one metrics row. The pre-existing "re-requests once on an empty completion" test now asserts the row lands on `turnShown` (was: immediately).
- **Commit** `db3102d fix(brain): canned reply follows the normal lifecycle; write chain keeps success and failure (GC-2, GC-3)` — GC-2 and the runner half of GC-3 share the lifecycle refactor (`settleFinished`, `assistantKind`, the enqueue signature), so they are one commit; the desktop half of GC-3 is separate.

### GC-3 [major] `turn.ts` enqueue/commitUser/send — the write chain swallowed failures
- **Fixed how.** `enqueue(turn, label, work)` (turn.ts:300) returns the **operation** promise (`this.writes.then(work)`, rejects) and continues the barrier on the caught one (`op.catch(warn + emit)`), so ordering never blocks on a failed write but no caller is told a failed write succeeded. Every failure is warned (`[turn] history write failed (<label>)`) and emitted **once** as the new runner event `persistFailed { turnId, label, message }` (turn.ts:34). `committed: boolean` is replaced by `userCommit: CommitState = 'none' | 'pending' | 'durable' | 'failed'` (turn.ts:73/110); `commitUser` flips it from the operation promise and returns a never-rejecting "settled" promise. `send()` supersede: `none`/`failed` → the text moves to the new turn as before; `pending` → the new turn records `inherit = previous` and `run()` awaits that commit after the write barrier and prepends the text **only if it failed** (turn.ts:325-330) — a durable row is never duplicated. `commitAssistant`/`persistShown` swallow the operation rejection locally (the failure was already reported by enqueue), so the terminal outcome (turnDone / idle) is unchanged, as A-26 requires, but the row's absence is now explicit.
- **Desktop surface** (`apps/desktop/src/main/brain-service.ts`:542-560): `BrainService` subscribes to `persistFailed` → `console.warn('[brain] history write failed (%s) turn=%s detail=%s', …, boundedDetail(message))` (redacted, 200 chars) and `hint:show { text: STORAGE_HINT_TEXT, level:'warn', ttlMs: HINT_TTL_MS }`. When the runner is idle the hint owns the window hide like `reportError`'s; mid-turn the hint rides along and playback keeps the hide. **Decision:** `ErrorCode` (`@ds/protocol`) has no `storage` member and protocol is outside this lane's ownership, so the surface is a hint, not a `brain:error{code:'storage'}` — and emitting a §3.11.5 `error` would be wrong anyway (the reply was spoken, turnDone went out). Amendment proposed below to add `storage` in a later protocol pass.
- **Tests** (`turn.test.ts`, describe "the write chain keeps success and failure (GC-3)"): pending user append rejects, then supersede → t2's user row is `'我回来了。\n那你先睡吧。'`, the prompt carries both, `persistFailed` = `[{t1, 'user row', 'SQLITE_FULL (user)'}]`; pending user append **succeeds**, then supersede → no duplication, no `persistFailed`; assistant append rejects → `persistFailed {t1, 'assistant row'}`, only the user row exists, state idle, no `error` event; interrupted row on cancel rejects → reported. `brain-service.test.ts` `GC-3: a persistFailed from the runner warns and shows the storage hint; idle -> the hint owns the hide` (also asserts the warn line does not contain an `sk-…` secret from the message, and that mid-turn the hide is not taken over).
- **Commits** `db3102d` (runner) and `9c6cc03 fix(desktop): surface a failed history append as a warning plus the storage hint (GC-3)`.

### GC-4 [major] `packages/brain/src/deepseek.ts` — malformed SSE frames skipped
- **Fixed how.** `parseSseLine` (deepseek.ts ~130): an empty `data:` payload is still ignored; a **non-empty** payload that is not a JSON object (parse failure, or valid JSON that is not an object such as `42`/`[]`) logs `[deepseek] malformed SSE frame (N chars)` and throws `MalformedFrameError` (deepseek.ts:104, a `DeepSeekError` with code `server`, status null, message with the length only, no `detail`). A frame whose object carries a non-null `error` member (provider error inside a 200 SSE body) throws `DeepSeekError('server', null, 'upstream error frame inside the stream')` after a length-only warning. Neither the warning, the message nor `detail` ever carries the payload. The throw propagates out of `streamOnce`'s read loop (body cancelled in its `finally`), and `streamWithRetries` applies the unchanged gate — retried only when no delta was seen. At EOF, a `MalformedFrameError` from the unterminated remainder is treated as G-2's truncation (`network`), not as a malformed frame (deepseek.ts:519): half a frame at EOF is a cut socket.
- **Tests** (`deepseek.test.ts`): `GC-4: a malformed non-empty data frame is a server error with exactly one console.warn, never skipped`; `GC-4: an empty data frame is still ignored`; `CX-8 / GC-4: … carry the frame length, never the payload bytes` (message, detail and log checked); `GC-4: a provider error object inside a data frame is a server error that never carries the payload` (with an `sk-…` in the error message); stream-level: `delta + malformed + [DONE]` rejects with `server` after yielding the first delta, one request (no retry after a delta), log has only `chars`; malformed before any delta → retried once and succeeds; provider error frame after a delta → `server`, message free of the upstream text. The G-2 EOF tests stay green.
- **Commit** `e0bfb3c fix(brain): a malformed or error-carrying SSE data frame fails the stream instead of being skipped (GC-4)`.

### GC-5 [major] `apps/desktop/src/main/brain-service.ts` — key:test result not tied to key/generation
- **Fixed how** (brain-service.ts:69-99, 168-197, 296-305, 459). Before the await the handler stamps `gen = this.keyGen` and `tested = keyFingerprint(literal ?? keyStore.get())` — the first 16 hex chars of sha-256, never the key. After the await: `disposed || gen !== this.keyGen` → return the result to the invoke caller only; `tested !== keyFingerprint(keyStore.get())` → same (an unsaved literal key never describes the stored one; a literal key that IS the stored key does); otherwise `lastTest` is set and `key:status` broadcast. `rebuildClient()` (every KeyStore change) calls `invalidateKeyTests()`: `keyGen += 1` and aborts every in-flight test's controller. New optional dep `BrainServiceDeps.probeClient(apiKey)` for the literal-key probe (default `new DeepSeekClient({apiKey})`, index.ts untouched); tests inject a controllable fake so no key:test can reach the network.
- **Tests** (`brain-service.test.ts`): `GC-5: a delayed test of literal key A, then a rotation to B, then A resolving -> B's status is untouched` (A's probe is aborted by the rotation, its 401 result still returns to the caller, no extra `key:status`, `lastTest` stays null); `GC-5: testing an unsaved literal key never updates the stored key's status` (and the literal-equals-stored case does update); `GC-5: a stored-key test that resolves after a rotation does not stamp the new key`. The M-7 test now drives the stored-key probe explicitly through the controllable client.
- **Commit** `c197200 fix(desktop): tie every key:test to its generation and tested key; abort and drain them on key change and dispose (GC-5, GC-6)` — GC-6 is the same tracking structure, one commit.

### GC-6 [minor] `brain-service.ts` dispose + key:test
- **Fixed how.** Every key:test is tracked in `keyTests` (controller + a never-rejecting `settled` promise, brain-service.ts:98/181-191). `dispose()` calls `invalidateKeyTests()` first (generation bumped, controllers aborted) and, after the runner cancel / retiring / trim barriers, `await Promise.all([...keyTests].map(t => t.settled))` (brain-service.ts:440-444) — inside the same `DRAIN_TIMEOUT_MS` bound quit.ts applies to `dispose()`. Every post-await continuation is guarded by `disposed` and the generation, so no `refreshKeyStatus()` runs against a closing window; the no-key branch is guarded too.
- **Test** `GC-6: dispose() aborts a pending key:test, waits for it to settle and sends no key:status afterwards` (probe held open past the abort → `dispose()` stays unresolved; after the probe resolves, dispose resolves and neither window received another `key:status`).
- **Commit** `c197200` (with GC-5).

### GC-7 [minor] `deepseek.ts testKey` — ignored `truncated`
- **Fixed how** (deepseek.ts:314, 578-586). `testKey()` reads the 200 body through `readBodyBounded` as before, then: `truncated` → `DeepSeekError('server', status, 'response body exceeded … bytes')` (surfaces as `{ok:false, code:'server'}`); body not matching `isCompletionShape` — a JSON object with a non-empty `choices` array whose first element has an object `message` — → `server` ('unreadable response body: not a chat completion'). Only then `{ok:true}`.
- **Tests**: `GC-7: an oversized 200 body is a server failure, never ok` (endless body, cancelled after the cap, bounded pulls); `GC-7: a 200 whose body is not a completion is a server failure` (`{}`, `not json`, `{"choices":[]}`, `{"choices":[{"delta":{}}]}`, `[]`). The pre-existing "returns ok on 200" fixture already had the completion shape.
- **Commit** `6fd7770 fix(brain): testKey() honours the bounded-body truncation and validates the completion shape (GC-7)`.

## Amendments proposed (for `docs/superpowers/plans/2026-08-29-phase2-contracts.md`, "Amendments" section)

- **A-37 §3.9.4 canned lifecycle (GC-2).** The §3.9.4 canned line is a reply like any other: emitted through the ordinary sentence path while the turn is unsettled, `turnDone` + `MetricsRecord{errorCode:'empty'}` at once, the `kind:'system'` assistant row written by `turnShown()` (A-27 applies verbatim). A `send()`/`cancel()`/`bubbleCrashed()` before that retires the turn and persists only the acknowledged canned sentence as `{kind:'system', interrupted:true}`; the retire's metrics row still carries `errorCode:'empty'`. There is no path on which the canned sentence is emitted after a newer turn replaced `current`.
- **A-38 §3.9 / §3.11.4 write states + storage surface (GC-3).** `TurnRunner` keeps two promises per history append: the operation promise (rejects) and the caught barrier (ordering only). The user row's state is `userCommit: 'none' | 'pending' | 'durable' | 'failed'`. A superseding `send()` carries the previous text forward when its row is `none` or `failed`; when it is `pending` the new turn awaits that write after the barrier and carries the text only if it failed. Every failed append is logged `[turn] history write failed (<label>)` and emitted once as `persistFailed { turnId, label, message }`; A-26's outcome guarantees (turnDone / error / idle unaffected) stand. Main handles `persistFailed` with one bounded, redacted warn line and `hint:show` `{ text: STORAGE_HINT_TEXT, level:'warn', ttlMs: HINT_TTL_MS }` (hide ownership: the hint when idle, the playback mid-turn). **Follow-up for a protocol-owning lane:** add `'storage'` to `ErrorCodeSchema` and `ERROR_HINTS` (`{ text: STORAGE_HINT_TEXT, level:'warn', opensKeyWindow:false }`) so this can travel as `brain:error{code:'storage'}` to the chat window too; until then it is a hint only.
- **A-39 §3.9.3 malformed-frame rule (GC-4).** Replaces "a malformed frame is skipped with one warning": a non-empty `data:` payload that is not a JSON object is `DeepSeekError('server')` (`MalformedFrameError`), and a JSON frame carrying a non-null `error` member inside a 200 stream is `DeepSeekError('server')`; both are logged with the frame length only and never carry the payload in `message` or `detail`; retry only via the existing no-delta gate. An empty `data:` payload is ignored. At EOF an unterminated remainder that fails to parse is A-23's `network` truncation, not a malformed frame. New table rows: `| malformed non-empty data frame | server | null | only via the existing gate |`, `| provider error object in a 200 frame | server | null | only via the existing gate |`. `testKey()`: a 200 is `ok` only when the bounded body is not truncated and parses as `{ choices: [ { message: object } ] }`; otherwise `{ok:false, code:'server'}` (GC-7).
- **A-40 §2.3 key:test generation rule (GC-5 / GC-6).** A `key:test` result updates `key:status.lastTest` only if (a) no `KeyStore` change happened since the test started (generation counter, bumped by `rebuildClient()` and by `dispose()`) and (b) the sha-256 fingerprint of the key it tested equals the fingerprint of the key stored now. A literal (unsaved) key test therefore never describes the stored key unless it is the same key. Every result is still returned to its invoke caller. In-flight tests are aborted on a key change and on `dispose()`, which awaits their settlement inside the A-29 drain; no continuation runs after `dispose()`. `BrainServiceDeps.probeClient?(apiKey)` is the injectable literal-key probe (default `DeepSeekClient`).
- **A-41 §4.3 / §4.4 trim-prefix rule (GC-1).** A trim retires every row `last_trim_id < id <= cutoff`, cutoff being A-33's exact id. The summariser receives ALL of them — the rows the `HISTORY_WINDOW_SAFETY_TOKENS` truncation kept out of `window()` included — in id order, once each, in sequential chunks of at most `TRIM_CHUNK_TOKENS = 24_000` estimated tokens (a single larger row is its own chunk), each chunk seeing the capped summary produced by the previous one. `last_trim_id` and the summary are committed together only after the last chunk; the commit re-validates the pointer snapshot and the id-bearing prefix snapshot; a failing chunk writes nothing.

## Decisions taken without asking (recorded per the brief)
1. GC-3's `storage` hint is a `hint:show`, not a new `ErrorCode` — protocol is outside the lane; amendment A-38 asks for the enum addition.
2. GC-1 summarises intermediate chunks in memory and commits once, rather than committing the summary per chunk — the brief allows either ("without advancing `last_trim_id`"); one commit is the atomic option.
3. GC-2 + GC-3 (runner) and GC-5 + GC-6 are single commits each because they are one refactor; each finding still has its own named tests.
4. GC-4 treats valid-JSON-non-object frames (`42`, `[]`) as malformed too, and an unterminated remainder at EOF as G-2's `network` truncation rather than `server`.
5. Existing tests whose assertions encoded the old (buggy) behaviour were updated, with a comment naming the finding: CX-5 (`summarizeCalls[0][1]`), the empty-completion canned-row test, and the two `parseSseLine` skip tests.
