prompt_cache_hit_tokens / prompt_tokens - one continuous session, turns 3+
source: NOT MEASURED - eval/session.mjs was never run against the real API
model: deepseek-v4-flash (never reached)
started: -
turns 3+ cache-hit tokens: NOT MEASURED
turns 3+ prompt tokens: NOT MEASURED
cache-hit ratio: NOT MEASURED (bar >= 70.0 %, X1)

reason: on 2026-08-29 the owner's DeepSeek account returns HTTP 402 "Insufficient Balance" for
every chat completion. The key authenticates - an invalid key returns 401, which the client maps
to the `auth` hint; this is the `balance` hint. Verified with the cheapest possible call:

  $env:DEEPSEEK_API_KEY = (Get-Content $HOME\.ds\deepseek.key -Raw).Trim()
  pnpm --filter @ds/eval run eval -- --limit 1 --runs 1 --no-judge
  -> DeepSeekError: {"error":{"message":"Insufficient Balance", ...}}

No per-turn cache figures exist. Nothing here is estimated, and no offline (--dry or
DS_FAKE_BRAIN) number is substituted: those clients do not talk to DeepSeek and therefore have no
prompt cache to hit. docs/evidence/phase2/not-measured.md carries the exact command list that
regenerates this file - it is derived from session-20-turns.json, so the two can never disagree.
